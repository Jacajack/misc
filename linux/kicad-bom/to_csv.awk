#!/usr/bin/awk -f
{
	if ($1 == "#")
	{
		printf("\"%s\", \"%s\", \"%s\", \"%s\"\n", "---", "---", "---", $2);
	}
	else
	{
		printf("\"%s\", \"%s\", \"%.2f\", \"%.2f\"\n", $4, $6, $7 / 10, $8 / 10);
	}
}
